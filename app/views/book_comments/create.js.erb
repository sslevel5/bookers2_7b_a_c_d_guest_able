$("#comment_count").html("<%= j(render 'book_comments/count', book: @comment.book) %>");
$("#comment_index").html("<%= j(render 'book_comments/index', book: @comment.book) %>");
<!--中身をすべて書き換えるのでインスタンス変数で値を渡す-->
$('#comment_text').val("");